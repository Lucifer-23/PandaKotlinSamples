package com.panda.kotlin.samples.code.chapter5

class Chapter5_16_3_1_ONE {

    fun main() {
        // sampleStart
        val numbers = listOf(1, 2, 3, 4, 2, 5)

        println(numbers.indexOf(2))
        println(numbers.lastIndexOf(2))
        // sampleEnd
    }
}